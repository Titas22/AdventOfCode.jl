module AoC_2025
    using AdventOfCode
    const AoC = AdventOfCode
    
    
    # file    = @__FILE__;
    
    # # folder     = @__DIR__;

    # (folder, filename)  = splitdir(file)
    # (fname, ext)        = splitext(filename)

    # println("Filename '$(filename)'   Extension: '$(ext)'")


    # files = readdir(folder, join=true)
    # println(folder)
    # println(file)

    # days        = AoC.DEFAULT_SOLVED_DAYS

    # allFiles    = map(x->joinpath(folder, "$(fname)_$(lpad(x,2,"0"))$(ext)"), days)
    # solvedFiles = intersect(allFiles, files)







    # TODO: Add Benchmarks here

    # include("./AoC_2025_01.jl")
    # # import  ..AoC_2025_01;
    # display(@benchmark (part1, part2) = AoC_2025_01.solve());

    # With input caching (each line as string)
    # @benchmark (part1, part2) = AoC_2025_01.solve(use_input_cache=true)
end 